import type { NextConfig } from "next";


// image host name configuration

const nextConfig: NextConfig = {
  images :{
    domains : ["avatars.githubusercontent.com"]
  }
  /* config options here */
  
};

export default nextConfig;
